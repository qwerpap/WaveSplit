class UIConstants {
  UIConstants._();

  static const double snackBarTopOffset = 16.0;
  static const double snackBarHorizontalPadding = 16.0;
  static const double networkOverlayTopOffset = 8.0;
  static const double networkOverlayHorizontalPadding = 16.0;
  
  static const Duration snackBarAnimationDuration = Duration(milliseconds: 300);
  static const Duration snackBarDefaultDuration = Duration(seconds: 3);
  static const Duration snackBarErrorDuration = Duration(seconds: 4);
  static const Duration snackBarShortDuration = Duration(seconds: 2);
}